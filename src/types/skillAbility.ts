export type SkillAbilityType = {
  totalAvgResponse: number
  skillTotalScoreResponse: {
    additionalProp1: {
      totalScore: number
      currentCount: number
      totalCount: number
    }
    additionalProp2: {
      totalScore: number
      currentCount: number
      totalCount: number
    }
    additionalProp3: {
      totalScore: number
      currentCount: number
      totalCount: number
    }
  }
}
