import { ExplanationInfo } from '@/types/quizresult'
import React from 'react'

export default function QuizReultCard({
  wordId,
  isCorrect,
  isMarked,
  name,
  selectedOptionDescription,
  answerOptionDescription,
}: ExplanationInfo) {
  return <div>QuizReultCard</div>
}
